<section class="register-page">
  <div class="register-container">
    <h1 class="register-title"><%= titulo %></h1>

    <form id="registerForm" class="register-form">
      <div class="form-group">
        <label for="name">Nome Completo</label>
        <div class="input-with-icon">
          <i class="material-icons">person_outline</i>
          <input type="text" id="name" name="name" placeholder="Seu nome completo" required>
        </div>
      </div>
      <div class="form-group">
        <label for="email">E-mail</label>
        <div class="input-with-icon">
          <i class="material-icons">mail_outline</i>
          <input type="email" id="email" name="email" placeholder="Seu e-mail" required>
        </div>
      </div>
      <div class="form-group">
        <label for="phone">Telefone</label>
        <div class="input-with-icon">
          <i class="material-icons">phone_iphone</i>
          <input type="text" id="phone" name="phone" placeholder="Seu telefone" required>
        </div>
      </div>
      <div class="form-group">
        <label for="password">Senha</label>
        <div class="input-with-icon">
          <i class="material-icons">lock_outline</i>
          <input type="password" id="password" name="password" placeholder="Crie uma senha" required>
        </div>
      </div>
      <div class="form-group">
        <label for="confirmPassword">Confirmar Senha</label>
        <div class="input-with-icon">
          <i class="material-icons">verified_user</i>
          <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Confirme sua senha" required>
        </div>
      </div>
      <button type="submit" class="btn-primary">Registrar</button>
    </form>

    <script>
      document.getElementById('registerForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        
        const form = e.target;
        const formData = new URLSearchParams(new FormData(form));

        if (formData.get('password') !== formData.get('confirmPassword')) {
          if (typeof showNotification === 'function') {
            showNotification('As senhas não coincidem.', 'error');
          } else {
            alert('As senhas não coincidem.');
          }
          return;
        }
        
        try {
          const response = await fetch('/register', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/x-www-form-urlencoded'
            },
            body: formData.toString()
          });
          
          const contentType = response.headers.get("content-type");
          let result;
          if (contentType && contentType.includes("application/json")) {
            result = await response.json();
          } else {
            result = await response.text();
          }
          
          if (response.ok) {
            console.log('Registro com sucesso:', result);
            if (typeof showNotification === 'function') {
                showNotification('Registro realizado com sucesso!', 'success');
            } else {
                alert('Registro realizado com sucesso!');
            }
            setTimeout(() => {
                window.location.href = '/'; // redireciona para login ou admin dashboard
            }, 1000);
          } else {
            console.error('Erro no registro:', result);
            if (typeof showNotification === 'function') {
                showNotification(result.message || 'Erro ao realizar registro', 'error');
            } else {
                alert(result.message || 'Erro ao realizar registro');
            }
          }
        } catch (error) {
          console.error('Erro de rede:', error);
          if (typeof showNotification === 'function') {
              showNotification('Erro de conexão ao tentar fazer registro.', 'error');
          } else {
              alert('Erro de conexão ao tentar fazer registro.');
          }
        }
      });
    </script>

    <p class="login-prompt">Já tem uma conta? <a href="/login" class="login-link" hx-get="/login" hx-target="#main-content" hx-swap="innerHTML" hx-push-url="true">Faça login aqui!</a></p>
  </div>
</section>

<style>
  .register-page {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 2rem;
    min-height: 78vh;
    background-color: var(--cor-fundo-site);
    padding: var(--espacamento-grande);
  }

  .register-container {
    background-color: #fff;
    padding:  1.5rem 2rem 2rem;
    border-radius: 16px;
    box-shadow: var(--sombra-suave);
    width: 100%;
    max-width: 430px;
    text-align: center;
  }

  .register-title {
    color: var(--cor-primaria);
    margin-bottom: 0.8rem;
    font-size: 1.8rem;
    font-family: var(--fonte-titulo);
  }

  .register-message {
    color: var(--cor-texto-medio);
    margin-bottom: var(--espacamento-grande);
  }

  .register-form .form-group {
    margin-bottom: 1.2rem;
    text-align: left;
    position: relative;
  }

  .register-form label {
    display: block;
    margin-bottom: var(--espacamento-xs);
    color: var(--cor-texto-escuro);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .input-with-icon {
    position: relative;
    display: flex;
    align-items: center;
  }

  .input-with-icon i {
    position: absolute;
    left: 12px;
    color: #999;
    font-size: 20px;
  }

  .register-form input[type="text"],
  .register-form input[type="email"],
  .register-form input[type="password"] {
    width: 100%;
    padding: 0.8rem 1rem 0.8rem 2.5rem;
    border: 1px solid var(--cor-borda);
    border-radius: 8px;
    box-sizing: border-box;
    font-size: 1rem;
    transition: var(--transicao-suave);
    background-color: #fafafa;
  }

  .register-form input[type="text"]:focus,
  .register-form input[type="email"]:focus,
  .register-form input[type="password"]:focus {
    border-color: var(--cor-primaria);
    outline: none;
    background-color: #fff;
    box-shadow: 0 0 0 4px rgba(var(--cor-primaria-rgb, 0, 0, 0), 0.1);
  }

  .register-form input:focus + i {
    color: var(--cor-primaria);
  }

  .btn-primary {
    background-color: var(--cor-secundaria);
    color: var(--cor-texto-claro);
    padding: 1rem var(--espacamento-md);
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 0.95rem;
    font-weight: bold;
    transition: all 0.3s ease;
    width: 100%;
    margin-top: 0.5rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
  }

  .btn-primary:hover {
    filter: brightness(110%);
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
  }

  .btn-primary:active {
    transform: translateY(0);
  }

  .login-link {
    color: var(--cor-primaria);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s ease;
  }

  .login-link:hover {
    text-decoration: underline;
    color: var(--cor-secundaria);
  }

  .login-prompt {
    margin-top: var(--espacamento-grande);
    color: var(--cor-texto-medio);
  }
</style>